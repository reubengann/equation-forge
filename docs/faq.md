Why have add and multiply move modes? Why can't the program infer the move type?

It _could_ for most expressions, but consider
$$ x = y $$
If we click and drag $x$ to the other side of the equation, should the result be $ 0 = y - x $ or $ 1 = y / x $?
