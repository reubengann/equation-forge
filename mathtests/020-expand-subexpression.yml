version: 1
name: Expand product in equation side
tags: [expand]
initial:
  latex: "a\\left(b+c\\right)=1"
steps:
  - name: expand left side product
    select:
      kind: node
      path: [1]
    action: expand
    expect:
      latex: "a b + a c = 1"
